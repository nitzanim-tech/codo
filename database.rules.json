{
  "rules": {
    "users": {
      ".read": "auth.token.email.endsWith('nitzanim.tech')",
      ".indexOn": "email",
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
